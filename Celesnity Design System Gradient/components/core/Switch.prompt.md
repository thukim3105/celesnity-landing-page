**Switch** — pill toggle for binary settings; the on-state track fills with the nebula gradient. Natural fit for the brand's Cosmos ⇄ Daybreak theme switch.

```jsx
<Switch checked={dark} onChange={setDark} label="Cosmos mode" />
```

Controlled: pass `checked` and handle `onChange(next)`.
